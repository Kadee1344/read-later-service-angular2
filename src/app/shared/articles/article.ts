export class Article {
  title: string;
  body: string;
  url: string;

  constructor(title: string, body: string, url: string) {
    this.title = title;
    this.body = body;
    this.url = url;
  }
}
